spring:
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          introspection-uri: https://dev.citizen.com.vn/oauth2/introspect
          client-id: vominhvo
          client-secret: P@ssw0rd
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/vote_db
    username: htadmin
    password: mysecretpassword
logging:
  level:
    root: INFO
    web: DEBUG
    org:
      springframework:
        web: DEBUG
        security: DEBUG